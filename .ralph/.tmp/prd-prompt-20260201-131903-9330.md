You are an autonomous coding agent.
Use the $prd skill to create a Product Requirements Document in JSON.
Save the PRD as JSON in directory: /Users/mac/codes/vibe-inspect/.agents/tasks
Filename rules: prd-<short-slug>.json using 1-3 meaningful words.
Examples: prd-workout-tracker.json, prd-usage-billing.json
Do NOT implement anything.
After creating the PRD, end with:
PRD JSON saved to <path>. Close this chat and run `ralph build`.

User request:
存在的几个问题，需要好好的修复：
1. vnc 的触摸板，注意是触摸板！功能不支持多点触摸，或者是没有把多点触摸的逻辑透传到pc agent中，反正就是我尝试使用mac的多点触摸板的功能，无法用，Windows的应该也是一样，应该是是没做多点触摸的功能
2. 在全屏的时候，增加一个键盘功能，可以打字输入内容，补齐没有字符input的段盘，要兼容全屏+竖屏，全屏+横屏的逻辑，UI要设计的符合相关View的风格！
3. mobile端vnc的debug的view，默认不显示，有一个开关可以显示，全屏模式下，有一个更多选项按钮可以显示，全屏竖屏，横屏模式都要有。
4. roi的健壮性，连通性，这些都要做的的完善，不要尝试做什么端口冲突的兼容，我们只使用在设置里面定义的端，如果冲突了应该启动报错，而不是换端口，因为后面我们要用frp，所以不能这样随机搞端口。
5. 如果放大到最大的时候，分配率再高一点就好了。
6. 放大缩小组件希望有一个可以回到100%的档位，比如接近95-105之间，就回到100，这样交互更好。
7. mobile端最好存放着一些日志功能，出问题可以通过日志分析到一些问题。
8. vnc view触摸板的鼠标左键，右键，支持长按的功能，正常的触摸板都支持长按，这样就你实现拖动的功能了，现在不支持

上面的内容，请你好好总结一下，该抽象的抽象，该重构的重构，先详细检索和了解底层和相关上下文的细节，不要改了a，导致b坏了，修复了c, a和b都坏了，好好了解上下文，然后再去做计划和方案。
请你以一名资深的架构师, rust, ios,macos,windows, android, flutter 开发专家和产品设计专家的角度去思考和实现。
遇到不明白和不会的，不理解的，应该去检索官方代码和文档，尽量尝试clone 代码遍历代码，而不是通过web的手段，低效。尽量不要自以为是的瞎猜。
