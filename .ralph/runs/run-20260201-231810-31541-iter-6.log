2026-02-01T16:35:59.098764Z ERROR codex_core::skills::loader: failed to stat skills entry /Users/mac/.codex/skills/dev-browser/profiles/browser-data/RunningChromeVersion (symlink): No such file or directory (os error 2)
2026-02-01T16:35:59.098817Z ERROR codex_core::skills::loader: failed to stat skills entry /Users/mac/.codex/skills/dev-browser/profiles/browser-data/SingletonCookie (symlink): No such file or directory (os error 2)
2026-02-01T16:35:59.098821Z ERROR codex_core::skills::loader: failed to stat skills entry /Users/mac/.codex/skills/dev-browser/profiles/browser-data/SingletonLock (symlink): No such file or directory (os error 2)
OpenAI Codex v0.93.0 (research preview)
--------
workdir: /Users/mac/codes/vibe-inspect
model: gpt-5.2-codex
provider: sub2api
approval: never
sandbox: danger-full-access
reasoning effort: high
reasoning summaries: auto
session id: 019c1a0f-a623-70d1-815e-2555a54f4259
--------
user
# Build

You are an autonomous coding agent. Your task is to complete the work for exactly one story and record the outcome.

## Paths
- PRD: /Users/mac/codes/vibe-inspect/.agents/tasks/prd-terminal-daemon.json
- AGENTS (optional): /Users/mac/codes/vibe-inspect/AGENTS.md
- Progress Log: /Users/mac/codes/vibe-inspect/.ralph/progress.md
- Guardrails: /Users/mac/codes/vibe-inspect/.ralph/guardrails.md
- Guardrails Reference: /Users/mac/codes/vibe-inspect/.agents/ralph/references/GUARDRAILS.md
- Context Reference: /Users/mac/codes/vibe-inspect/.agents/ralph/references/CONTEXT_ENGINEERING.md
- Errors Log: /Users/mac/codes/vibe-inspect/.ralph/errors.log
- Activity Log: /Users/mac/codes/vibe-inspect/.ralph/activity.log
- Activity Logger: /Users/mac/codes/vibe-inspect/ralph log
- No-commit: false
- Repo Root: /Users/mac/codes/vibe-inspect
- Run ID: 20260201-231810-31541
- Iteration: 6
- Run Log: /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260201-231810-31541-iter-6.log
- Run Summary: /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260201-231810-31541-iter-6.md

## Global Quality Gates (apply to every story)
- cd mobile && flutter test
- cd mobile && flutter analyze
- ./scripts/flutter_test.sh
- ./scripts/flutter_analyze.sh

## Selected Story (Do not change scope)
ID: US-006
Title: Attach/poll/input streaming + compatibility payloads

Story details:
### US-006: Attach/poll/input streaming + compatibility payloads
Status: in_progress
Depends on: US-005

Description:
As a client, I want attach/poll/input so I can stream output and send input while keeping payload compatibility.

Acceptance Criteria:
- [ ] Implement `attach`, `detach`, `poll`, and `input` actions with the specified envelope
- [ ] Attach adds a subscription and immediately emits a terminal_payload event with output since `since`
- [ ] `poll` returns terminal_payload with output since `since` and notifications since `notify_since`
- [ ] Example: `input` of "ls -la\n" results in output chunks with incremented seq
- [ ] Negative: providing both `data` and `data_b64` yields `invalid_request`


If the story details are empty or missing, STOP and report that the PRD story format could not be parsed.

## Rules (Non-Negotiable)
- Implement **only** the work required to complete the selected story.
- Complete all tasks associated with this story (and only this story).
- Do NOT ask the user questions.
- Do NOT change unrelated code.
- Do NOT assume something is unimplemented — confirm by reading code.
- Implement completely; no placeholders or stubs.
- If No-commit is true, do NOT commit or push changes.
- Do NOT edit the PRD JSON (status is handled by the loop).
- All changes made during the run must be committed (including updates to progress/logs).
 - Before committing, perform a final **security**, **performance**, and **regression** review of your changes.

## Your Task (Do this in order)
1. Read /Users/mac/codes/vibe-inspect/.ralph/guardrails.md before any code changes.
2. Read /Users/mac/codes/vibe-inspect/.ralph/errors.log for repeated failures to avoid.
3. Read /Users/mac/codes/vibe-inspect/.agents/tasks/prd-terminal-daemon.json for global context (do not edit).
4. Fully audit and read all necessary files to understand the task end-to-end before implementing. Do not assume missing functionality.
5. If /Users/mac/codes/vibe-inspect/AGENTS.md exists, follow its build/test instructions.
6. Implement only the tasks that belong to US-006.
7. Run verification commands listed in the story, the global quality gates, and in /Users/mac/codes/vibe-inspect/AGENTS.md (if required).
8. If the project has a build or dev workflow, run what applies:
   - Build step (e.g., `npm run build`) if defined.
   - Dev server (e.g., `npm run dev`, `wrangler dev`) if it is the normal validation path.
   - Confirm no runtime/build errors in the console.
9. Perform a brief audit before committing:
   - **Security:** check for obvious vulnerabilities or unsafe handling introduced by your changes.
   - **Performance:** check for avoidable regressions (extra queries, heavy loops, unnecessary re-renders).
   - **Regression:** verify existing behavior that could be impacted still works.
10. If No-commit is false, commit changes using the `$commit` skill.
    - Stage everything: `git add -A`
    - Confirm a clean working tree after commit: `git status --porcelain` should be empty.
    - After committing, capture the commit hash and subject using:
      `git show -s --format="%h %s" HEAD`.
11. Append a progress entry to /Users/mac/codes/vibe-inspect/.ralph/progress.md with run/commit/test details (format below).
    If No-commit is true, skip committing and note it in the progress entry.

## Progress Entry Format (Append Only)
```
## [Date/Time] - US-006: Attach/poll/input streaming + compatibility payloads
Thread: [codex exec session id if available, otherwise leave blank]
Run: 20260201-231810-31541 (iteration 6)
Run log: /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260201-231810-31541-iter-6.log
Run summary: /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260201-231810-31541-iter-6.md
- Guardrails reviewed: yes
- No-commit run: false
- Commit: <hash> <subject> (or `none` + reason)
- Post-commit status: `clean` or list remaining files
- Verification:
  - Command: <exact command> -> PASS/FAIL
  - Command: <exact command> -> PASS/FAIL
- Files changed:
  - <file path>
  - <file path>
- What was implemented
- **Learnings for future iterations:**
  - Patterns discovered
  - Gotchas encountered
  - Useful context
---
```

## Completion Signal
Only output the completion signal when the **selected story** is fully complete and verified.
When the selected story is complete, output:
<promise>COMPLETE</promise>

Otherwise, end normally without the signal.

## Additional Guardrails
- When authoring documentation, capture the why (tests + implementation intent).
- If you learn how to run/build/test the project, update /Users/mac/codes/vibe-inspect/AGENTS.md briefly (operational only).
- Keep AGENTS operational only; progress notes belong in /Users/mac/codes/vibe-inspect/.ralph/progress.md.
- If you hit repeated errors, log them in /Users/mac/codes/vibe-inspect/.ralph/errors.log and add a Sign to /Users/mac/codes/vibe-inspect/.ralph/guardrails.md using /Users/mac/codes/vibe-inspect/.agents/ralph/references/GUARDRAILS.md as the template.

## Activity Logging (Required)
Log major actions to /Users/mac/codes/vibe-inspect/.ralph/activity.log using the helper:
```
/Users/mac/codes/vibe-inspect/ralph log "message"
```
Log at least:
- Start of work on the story
- After major code changes
- After tests/verification
- After updating progress log

## Browser Testing (Required for Frontend Stories)
If the selected story changes UI, you MUST verify it in the browser:
1. Load the `dev-browser` skill.
2. Navigate to the relevant page.
3. Verify the UI changes work as expected.
4. Take a screenshot if helpful for the progress log.

A frontend story is NOT complete until browser verification passes.

mcp startup: no servers

thinking
**Preparing to review guidelines and docs**
exec
/bin/zsh -lc ls in /Users/mac/codes/vibe-inspect succeeded in 19ms:
AGENTS.md
design-system
design.md
desktop
docs
mobile
README.md
scripts
exec
/bin/zsh -lc 'cat /Users/mac/codes/vibe-inspect/.ralph/guardrails.md' in /Users/mac/codes/vibe-inspect succeeded in 24ms:
# Guardrails (Signs)

> Lessons learned from failures. Read before acting.

## Core Signs

### Sign: Read Before Writing
- **Trigger**: Before modifying any file
- **Instruction**: Read the file first
- **Added after**: Core principle

### Sign: Test Before Commit
- **Trigger**: Before committing changes
- **Instruction**: Run required tests and verify outputs
- **Added after**: Core principle

---

## Learned Signs

exec
/bin/zsh -lc 'cat /Users/mac/codes/vibe-inspect/.ralph/errors.log' in /Users/mac/codes/vibe-inspect succeeded in 22ms:
# Error Log

> Failures and repeated issues. Use this to add guardrails.

[2026-01-27 23:21:58] ITERATION 1 left uncommitted changes; review run summary at /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260127-230040-27989-iter-1.md
[2026-01-27 23:34:09] ITERATION 2 left uncommitted changes; review run summary at /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260127-230040-27989-iter-2.md
[2026-01-28 00:00:23] ITERATION 3 command failed (status=1)
[2026-01-28 00:00:23] ITERATION 3 left uncommitted changes; review run summary at /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260127-230040-27989-iter-3.md
[2026-01-28 00:00:23] ITERATION 3 exited non-zero; review /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260127-230040-27989-iter-3.log
[2026-01-28 00:00:58] ITERATION 4 command failed (status=1)
[2026-01-28 00:00:58] ITERATION 4 left uncommitted changes; review run summary at /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260127-230040-27989-iter-4.md
[2026-01-28 00:00:58] ITERATION 4 exited non-zero; review /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260127-230040-27989-iter-4.log
[2026-01-28 00:01:33] ITERATION 5 command failed (status=1)
[2026-01-28 00:01:33] ITERATION 5 left uncommitted changes; review run summary at /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260127-230040-27989-iter-5.md
[2026-01-28 00:01:33] ITERATION 5 exited non-zero; review /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260127-230040-27989-iter-5.log
[2026-01-28 00:02:09] ITERATION 6 command failed (status=1)
[2026-01-28 00:02:09] ITERATION 6 left uncommitted changes; review run summary at /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260127-230040-27989-iter-6.md
[2026-01-28 00:02:09] ITERATION 6 exited non-zero; review /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260127-230040-27989-iter-6.log
[2026-01-28 00:02:43] ITERATION 7 command failed (status=1)
[2026-01-28 00:02:43] ITERATION 7 left uncommitted changes; review run summary at /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260127-230040-27989-iter-7.md

thinking
**Planning to read PRD JSON**
exec
/bin/zsh -lc 'cat /Users/mac/codes/vibe-inspect/.agents/tasks/prd-terminal-daemon.json' in /Users/mac/codes/vibe-inspect succeeded in 23ms:
{
  "version": 1,
  "project": "Terminal Daemon + vibe-ctl",
  "overview": "Implement a dedicated terminal daemon (terminald) with a WebSocket-first protocol, plus a tmux-like CLI (vibe-ctl) and a desktop agent proxy that preserves existing desktop/mobile payload compatibility. Sessions must survive desktop agent restarts and follow the protocol and limits defined in docs/terminal_daemon_plan.md.",
  "goals": [
    "Terminal sessions survive desktop agent restarts by moving PTY ownership to terminald",
    "Provide a WebSocket protocol that matches the specified envelope, actions, events, and compatibility payload",
    "Deliver a tmux-like CLI (vibe-ctl) for listing, attaching, sending input, resizing, renaming, and stopping sessions",
    "Maintain existing payload schema compatibility for desktop/mobile UIs",
    "Cross-platform support for macOS, Windows, and Linux"
  ],
  "nonGoals": [
    "Multi-pane terminal support",
    "Remote (non-localhost) terminald access by default",
    "Recording/replay or long-term output persistence",
    "UI redesigns beyond optional warning/status surfaces",
    "Multi-user access control beyond single token auth"
  ],
  "successMetrics": [
    "Sessions remain active after desktop agent restart (verified by attach and output continuity)",

thinking
**Opening AGENTS documentation**
exec
/bin/zsh -lc 'cat /Users/mac/codes/vibe-inspect/AGENTS.md' in /Users/mac/codes/vibe-inspect succeeded in 22ms:
# AGENTS

## Verification
- `cd mobile && flutter test`
- `cd mobile && flutter analyze`
- `./scripts/flutter_test.sh`
- `./scripts/flutter_analyze.sh`
## Browser UI Check
- `cd mobile && flutter build web`
- `cd mobile/build/web && python3 -m http.server 8030 --bind 127.0.0.1`

thinking
**Starting US-006 investigation**

I'm preparing to inspect the codebase for US-006, focusing on Rust components like terminald and terminal_core to understand what changes are needed.
exec
/bin/zsh -lc 'rg -n "attach|detach|poll|input" desktop/src -S' in /Users/mac/codes/vibe-inspect succeeded in 33ms:
desktop/src/bin/terminald.rs:357:                input: None,
desktop/src/bin/terminald.rs:399:                input: None,
desktop/src/terminal_core.rs:417:    pub input: Option<String>,
desktop/src/terminal_core.rs:617:        "poll" => poll_session(request),
desktop/src/terminal_core.rs:619:        "input" => input_session(request),
desktop/src/terminal_core.rs:795:                            "input" => {
desktop/src/terminal_core.rs:796:                                if let Some(input) = parsed.get("data").and_then(|value| value.as_str()) {
desktop/src/terminal_core.rs:798:                                        action: "input".to_string(),
desktop/src/terminal_core.rs:801:                                        input: Some(input.to_string()),
desktop/src/terminal_core.rs:821:                                        input: None,
desktop/src/terminal_core.rs:838:                                    input: None,
desktop/src/terminal_core.rs:854:                                    input: None,
desktop/src/terminal_core.rs:975:    if let Some(input) = request.input.as_ref() {
desktop/src/terminal_core.rs:976:        if !input.is_empty() {
desktop/src/terminal_core.rs:981:                        let _ = session.writer.write_all(input.as_bytes());
desktop/src/terminal_core.rs:1009:fn poll_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
desktop/src/terminal_core.rs:1039:        "poll",
desktop/src/terminal_core.rs:1055:fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
desktop/src/terminal_core.rs:1057:    let input = request
desktop/src/terminal_core.rs:1058:        .input

thinking
**Inspecting terminal core changes**
exec
/bin/zsh -lc "sed -n '1,220p' /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs" in /Users/mac/codes/vibe-inspect succeeded in 24ms:
//! Shared terminal daemon core types and defaults.

use axum::extract::ws::{Message, WebSocket};
use directories::ProjectDirs;
use futures_util::{SinkExt, StreamExt};
use portable_pty::{native_pty_system, CommandBuilder, MasterPty, PtySize};
use rand::{distributions::Alphanumeric, Rng};
use serde::{Deserialize, Serialize};
use serde_json::{json, Value};
use std::collections::{HashMap, HashSet, VecDeque};
use std::fs;
use std::io::{Read, Write};
#[cfg(unix)]
use std::os::unix::fs::{OpenOptionsExt, PermissionsExt};
use std::path::{Path, PathBuf};
use std::sync::{Arc, Mutex, OnceLock};
use std::thread;
use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
use tokio::time::{interval, MissedTickBehavior};
use vt100;
exec
/bin/zsh -lc "sed -n '220,520p' /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs" in /Users/mac/codes/vibe-inspect succeeded in 19ms:
struct TerminalSession {
    id: String,
    label: String,
    cols: u16,
    rows: u16,
    created_at: u64,
    last_activity: u64,
    status: TerminalSessionStatus,
    exit_code: Option<i32>,
    buffer: VecDeque<TerminalOutputChunk>,
    buffer_bytes: usize,
    next_seq: u64,
    notification_seq: u64,
    notifications: VecDeque<TerminalNotification>,
    notification_cache: HashMap<String, u64>,
    parser: vt100::Parser,
    utf8_carry: Vec<u8>,
    master: Box<dyn MasterPty + Send>,
    child: Box<dyn portable_pty::Child + Send>,
    writer: Box<dyn Write + Send>,
exec
/bin/zsh -lc "sed -n '520,980p' /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs" in /Users/mac/codes/vibe-inspect succeeded in 17ms:

pub fn terminal_manager() -> &'static Arc<Mutex<TerminalManager>> {
    TERMINAL_MANAGER.get_or_init(|| {
        let manager = Arc::new(Mutex::new(TerminalManager::new()));
        spawn_cleanup(manager.clone());
        manager
    })
}

fn build_session_summary(session: &TerminalSession) -> TerminalSessionSummary {
    let last_output = session
        .buffer
        .back()
        .map(|chunk| chunk.data.trim().to_string())
        .unwrap_or_default();
    let preview = if last_output.chars().count() > 160 {
        last_output
            .chars()
            .rev()
            .take(160)
exec
/bin/zsh -lc "sed -n '980,1380p' /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs" in /Users/mac/codes/vibe-inspect succeeded in 23ms:
                    if let Ok(mut session) = session.lock() {
                        let _ = session.writer.write_all(input.as_bytes());
                        let _ = session.writer.flush();
                    }
                }
            }
        }
    }
    if let Ok(session) = session.lock() {
        persist_session_summary(&session);
    }

    Ok(build_session_payload(
        "start",
        &session_id,
        TerminalSessionStatus::Running,
        0,
        Vec::new(),
        None,
        false,
exec
/bin/zsh -lc "sed -n '1380,1840p' /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs" in /Users/mac/codes/vibe-inspect succeeded in 23ms:
    let short = session_id.chars().take(6).collect::<String>();
    format!("Terminal {}", short)
}

fn truncate_message(message: &str, max_len: usize) -> String {
    if message.chars().count() <= max_len {
        return message.to_string();
    }
    if max_len <= 3 {
        return message.chars().take(max_len).collect();
    }
    let mut truncated = message.chars().take(max_len - 3).collect::<String>();
    truncated.push_str("...");
    truncated
}

fn spawn_reader(session: Arc<Mutex<TerminalSession>>, mut reader: Box<dyn Read + Send>) {
    thread::spawn(move || {
        let mut buffer = [0u8; 4096];
        loop {

thinking
**Investigating terminal attach/detach**

Attach and detach methods aren't in the core terminal command handler but likely reside in the separate terminal socket serving, possibly managed by terminald; next I'll inspect terminald to confirm.
exec
/bin/zsh -lc "sed -n '1,240p' /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs" in /Users/mac/codes/vibe-inspect succeeded in 24ms:
use axum::{
    extract::{
        ws::{Message, WebSocket, WebSocketUpgrade},
        ConnectInfo, State,
    },
    http::StatusCode,
    response::IntoResponse,
    routing::get,
    Router,
};
use clap::Parser;
use futures_util::{SinkExt, StreamExt};
use rand::{distributions::Alphanumeric, Rng};

use desktop::terminal_core::{
    handle_terminal_command, list_terminal_sessions, write_discovery_file,
    TerminalActionRequest, TerminalDiscoveryFile, TerminalError, DEFAULT_TERMINALD_BIND,
    DEFAULT_TERMINALD_WS_PATH, TERMINALD_PROTOCOL_VERSION,
};
use serde_json::{json, Value};
exec
/bin/zsh -lc "sed -n '240,520p' /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs" in /Users/mac/codes/vibe-inspect succeeded in 22ms:
    let response = json!({
        "type": "res",
        "id": request_id,
        "ok": true,
        "data": {
            "server_info": {
                "version": state.version,
                "server_time": now_ts(),
                "capabilities": state.capabilities,
            }
        }
    });
    if send_json(&mut socket, response).await.is_err() {
        return;
    }

    run_authenticated_socket(&mut socket).await;
}

async fn run_authenticated_socket(socket: &mut WebSocket) {

thinking
**Identifying missing terminal actions**

terminald lacks attach/detach/poll/input actions needed for websocket handling, so I'll review docs/terminal_daemon_plan.md to understand envelope expectations before implementing these.
exec
/bin/zsh -lc 'rg -n "attach" /Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md' in /Users/mac/codes/vibe-inspect succeeded in 22ms:
4:We will build a dedicated terminal daemon (`terminald`) that owns PTY session lifecycle and output buffers. A standalone CLI (`vibe-ctl`) provides tmux-like commands (ls, attach, send, resize, stop). The desktop agent becomes a thin client to terminald and continues to serve mobile using the same payload shape, keeping mobile changes minimal. terminald uses WebSocket for all control and streaming; the desktop agent may continue to expose existing HTTP/WS endpoints for mobile compatibility.
42:- Provides tmux-like commands and attach/detach UX.
77:  "capabilities": ["attach", "poll", "notifications"]
104:  "action": "auth|start|list|attach|detach|poll|input|resize|rename|stop|keepalive",
155:      "capabilities": ["attach", "poll", "notifications"]
162:For session-scoped actions (`attach`, `detach`, `poll`, `input`, `resize`, `rename`, `stop`, `keepalive`), `session_id` is required in the request envelope.
181:#### attach
186:Server starts streaming `terminal_payload` events for this session. Multiple clients may attach.
293:- `attach` adds a subscription and immediately emits a `terminal_payload` event with:
298:- Multiple sockets may attach to the same session; fan-out is supported.
303:- On reconnect, call `attach` with last seen `since` and `notify_since`.
490:`attach`:
494:- response: `{ attached: true }` and begins push events on this socket
534:- `attach` is idempotent per `(socket, session_id)`; repeated calls are no-ops.
549:- Max attached sockets per session: `8`
623:- `vibe-ctl a -t <id>` / `vibe-ctl attach -t <id>`  
626:  Send input without attach.
654:- `attach`:
676:vibe-ctl attach -t abc
692:### 2) vibe-ctl new + attach
exec
/bin/zsh -lc "sed -n '140,260p' /Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md" in /Users/mac/codes/vibe-inspect succeeded in 24ms:
```json
{ "type": "req", "id": "uuid", "action": "auth", "payload": { "token": "..." } }
```
Server replies with `ok: true` or `ok: false` and an error code.
If auth fails or times out, the server should include a helpful error message before closing the connection; clients may reconnect and retry.
On success, server may include `server_info`:
```json
{
  "type": "res",
  "id": "uuid",
  "ok": true,
  "data": {
    "server_info": {
      "version": "1.0",
      "server_time": 1700000000,
      "capabilities": ["attach", "poll", "notifications"]
    }
  }
}
```
exec
/bin/zsh -lc "sed -n '260,360p' /Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md" in /Users/mac/codes/vibe-inspect succeeded in 24ms:
- `next_seq` is the next expected sequence number (last delivered `seq` + 1).
- If buffer truncated, server sets `truncated: true` and includes `snapshot`.
- Snapshot is vt100-rendered screen state plus cursor state.
- Clients must reset local output state on `truncated: true`.

### Errors
Errors are returned in response as:
```json
{ "code": "session_not_found", "message": "Session not found." }
```
Error codes align with existing desktop errors (`missing_input`, `resize_failed`, `pty_error`, etc.).

Common error codes:
- `invalid_auth`, `auth_required`
- `session_not_found`, `session_exists`, `session_ended`
- `missing_input`, `missing_size`, `invalid_size`
- `invalid_label`
- `write_failed`, `resize_failed`, `pty_error`, `spawn_error`
- `state_locked`, `invalid_request`, `unsupported_action`
- `client_backpressure`, `payload_too_large`

thinking
**Assessing terminald attach/detach handling**

I'm clarifying that terminal_core has poll/input but lacks attach/detach, which terminald likely must handle via socket subscriptions and event sends. Next, I'll search for terminald structures managing websocket streams or payloads to guide implementing US-006 attach/detach actions.
exec
/bin/zsh -lc 'rg -n "terminal_payload|stream|attach" desktop/src -S' in /Users/mac/codes/vibe-inspect succeeded in 34ms:
desktop/src/quic/mod.rs:235:        run_roi_stream(send_connection, info, capture_state, capture_running);
desktop/src/quic/mod.rs:246:    recv: &mut quinn::RecvStream,
desktop/src/quic/mod.rs:258:    send: &mut quinn::SendStream,
desktop/src/quic/mod.rs:283:fn run_roi_stream(
desktop/src/quic/mod.rs:578:        transport.max_concurrent_bidi_streams(100u32.into());
desktop/src/vnc.rs:5:use futures_util::{SinkExt, StreamExt};
desktop/src/vnc.rs:326:                "Screen recording permission is required to stream the desktop. Enable it in System Settings > Privacy & Security > Screen Recording, then restart the desktop agent."
desktop/src/vnc.rs:1667:    receiver: &mut futures_util::stream::SplitStream<WebSocket>,
desktop/src/terminal_core.rs:5:use futures_util::{SinkExt, StreamExt};
desktop/src/terminal_core.rs:271:        let text = decode_utf8_stream(&mut self.utf8_carry, bytes);
desktop/src/terminal_core.rs:647:    if let Err(error) = run_terminal_stream(socket, session, session_id).await {
desktop/src/terminal_core.rs:648:        eprintln!("terminal stream error: {error}");
desktop/src/terminal_core.rs:652:async fn run_terminal_stream(
desktop/src/terminal_core.rs:691:        "stream",
desktop/src/terminal_core.rs:760:                    "stream",
desktop/src/terminal_core.rs:1537:fn decode_utf8_stream(carry: &mut Vec<u8>, bytes: &[u8]) -> String {
desktop/src/bin/terminald.rs:12:use futures_util::{SinkExt, StreamExt};

thinking
**Identifying event envelope structure**

No attach/detach exists in terminald yet, so I'll implement them there and investigate the event envelope format, which likely uses a request/response pattern with a type field to distinguish event messages.
exec
/bin/zsh -lc 'rg -n "event" /Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md' in /Users/mac/codes/vibe-inspect succeeded in 18ms:
97:- `event`: server push event (clients must ignore unknown `event` values)
125:  "type": "event",
126:  "event": "terminal_payload|session_update|heartbeat|stream_paused|stream_resumed|session_warning",
186:Server starts streaming `terminal_payload` events for this session. Multiple clients may attach.
193:Server stops sending events for that session to this socket.
237:This object is emitted as `event: terminal_payload` and as `poll` response data. It matches current desktop payloads at the schema level to keep UI stable:
286:  1) emit `event: stream_paused` with a reason,
288:  3) resume with `event: stream_resumed` once caught up.
293:- `attach` adds a subscription and immediately emits a `terminal_payload` event with:
296:- If truncation occurs, the server should also emit `event: session_warning` with a `buffer_truncated` reason.
308:- Server may emit `event: heartbeat` every 15s when idle.
311:- The server may emit `event: session_warning` with a `reason` (e.g., `buffer_truncated`, `payload_truncated`, `payload_too_large`, `idle_expiring`, `slow_consumer`) and human-readable `message`.
315:- `event: stream_paused` data: `{ reason, retry_after_ms? }`
316:- `event: stream_resumed` data: `{}` (indicates streaming has resumed)
351:`session_warning` event (data object):
370:`stream_paused` event (data object):
383:`stream_resumed` event (data object):
419:Clients must ignore unknown fields in responses and events.
494:- response: `{ attached: true }` and begins push events on this socket
498:- response: `{ detached: true }` and stops push events for this session on this socket
exec
/bin/zsh -lc "sed -n '80,140p' /Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md" in /Users/mac/codes/vibe-inspect succeeded in 24ms:

Clients may override this via environment variables:
- `VIBE_CTL_ENDPOINT` (WS URL)
- `VIBE_CTL_TOKEN` (auth token)
- `VIBE_CTL_CONFIG` (path to discovery file)

### Connection
- Localhost WebSocket endpoint (TCP by default; optional UDS on supported OSes).
- Client must send an `auth` message immediately after connect.
- All subsequent messages are JSON.
- If `auth` is not received within 3 seconds, the server should reply with an auth error and then close the socket.
- Server rejects non-localhost connections by default.

### Message Envelope
All messages are JSON objects. There are three top-level `type`s:
- `req`: request/response RPC
- `res`: response to a request
- `event`: server push event (clients must ignore unknown `event` values)

Request:


thinking
**Planning multi-subscription streaming**

I'm deciding to implement attach/detach/poll/input actions with event envelopes in terminald, supporting multiple subscriptions per socket to handle streaming terminal output, emitting initial payloads on attach and ongoing output events. I'll verify how terminald currently processes requests sequentially to ensure compatibility.
thinking
**Planning subscription state and event streaming**

I'm planning to add state in run_authenticated_socket to track subscriptions and send terminal_payload events concurrently while processing incoming requests using the envelope format. I'll adapt streaming and input handling from terminal_core but implement multi-subscription event sends and a new function to build streaming payloads with action "stream" to fit terminald’s protocol without duplicating logic.
exec
/bin/zsh -lc "sed -n '1,220p' /Users/mac/codes/vibe-inspect/desktop/src/command.rs" in /Users/mac/codes/vibe-inspect succeeded in 25ms:
use crate::terminal;
use reqwest::blocking::Client;
use reqwest::Method;
use serde::de::DeserializeOwned;
use serde::{Deserialize, Serialize};
use serde_json::{json, Value};
use std::collections::HashMap;
use std::time::Instant;

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct AgentCommandRequest {
    pub request_id: String,
    pub command: String,
    pub payload: Option<Value>,
}

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct AgentCommandResponse {
    pub request_id: String,
    pub status: AgentCommandStatus,


thinking
**Evaluating terminal_core reuse for terminald**

Identified that terminal_core handles terminal streams but lacks attach/detach logic needed for terminald, which can manage attachments itself while possibly adding helpers in terminal_core for payload building; considering leveraging existing stream logic with modifications and clarifying boundaries between terminal_core and terminald responsibilities.
thinking
**Considering adding payload builder to terminal_core**

Planning to add a new function in terminal_core to build terminal payloads with flexible actions like "stream" by modifying or extending existing poll handling, acknowledging complexity with internal types; next step is to check for relevant tests and explore code for terminald architecture.
exec
/bin/zsh -lc 'rg -n "Terminald" -S /Users/mac/codes/vibe-inspect/desktop/src' in /Users/mac/codes/vibe-inspect succeeded in 29ms:
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:28:struct TerminaldArgs {
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:38:struct TerminaldState {
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:46:    let args = TerminaldArgs::parse();
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:73:    let state = TerminaldState {
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:120:    State(state): State<TerminaldState>,
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:130:async fn handle_socket(mut socket: WebSocket, state: TerminaldState) {


thinking
**Clarifying event timing and sequence handling for attach**

The attach API responds with {attached:true} and must immediately emit a terminal_payload event with output since the provided sequence. To avoid skipping output, the last sequence tracking should align so output_since(last_seq) includes any new data post-attach without missing events.
thinking
**Planning reuse of poll logic for streaming with attach**

Reusing poll_session to build attach streaming payload simplifies getting consistent output and notification handling by supplying the since and notify_since values. Adjusting the action field to "stream" after polling maintains payload structure while tracking last activity updates is acceptable. Also noting input requests must reject payloads containing both data and data_b64, supporting base64 decoding if data_b64 is present.
exec
/bin/zsh -lc 'rg -n "base64" /Users/mac/codes/vibe-inspect/desktop/Cargo.toml' in /Users/mac/codes/vibe-inspect exited 1 in 28ms:


thinking
**Checking dependencies for base64**
exec
/bin/zsh -lc 'cat /Users/mac/codes/vibe-inspect/desktop/Cargo.toml' in /Users/mac/codes/vibe-inspect succeeded in 21ms:
[package]
name = "desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"
hostname = "0.3"
qrcode = "0.13"
reqwest = { version = "0.12", features = ["blocking", "json"] }
get_if_addrs = "0.5"
portable-pty = "0.8"
axum = { version = "0.8", features = ["ws"] }
futures-util = "0.3"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }
tower-http = { version = "0.5", features = ["cors"] }
scrap = "0.5"


thinking
**Assessing base64 support and poll response**

Supporting base64-encoded input would improve compatibility, so adding a lightweight base64 crate and checking for exclusive use of data or data_b64 makes sense. Also, poll responses must embed a terminal_payload object, requiring routing poll requests properly in terminald.
thinking
**Designing subscription management and event streaming**

Planning to split the WebSocket sender/receiver to manage per-session subscriptions with tracked sequences and send terminal_payload events only on new output or status changes, matching run_terminal_stream logic. Attach will respond immediately then emit an event; detach will remove subscriptions. This requires restructuring to handle concurrent sending safely and maintain per-connection subscription state.
exec
/bin/zsh -lc 'rg -n "data_b64|terminal_payload|attach" /Users/mac/codes/vibe-inspect -S' in /Users/mac/codes/vibe-inspect succeeded in 35ms:
/Users/mac/codes/vibe-inspect/mobile/lib/main.dart:7463:    final ready = await _ensureRemoteSession(active, forceAttach: true);
/Users/mac/codes/vibe-inspect/mobile/lib/main.dart:7847:  Future<bool> _attachRemoteSession(TerminalSessionView view) async {
/Users/mac/codes/vibe-inspect/mobile/lib/main.dart:7913:          return _attachRemoteSession(view);
/Users/mac/codes/vibe-inspect/mobile/lib/main.dart:7931:    bool forceAttach = false,
/Users/mac/codes/vibe-inspect/mobile/lib/main.dart:7937:    if ((status == 'disconnected' || status == 'error') && forceAttach) {
/Users/mac/codes/vibe-inspect/mobile/lib/main.dart:7938:      return _attachRemoteSession(view);
/Users/mac/codes/vibe-inspect/mobile/lib/main.dart:7978:      final ready = await _ensureRemoteSession(active, forceAttach: force);
/Users/mac/codes/vibe-inspect/docs/bugfix/terminal_open_and_high_perf_review.md:12:   - Impact: session is attached to a different agent than the screen expects, so the terminal view loads empty or errors.
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:4:We will build a dedicated terminal daemon (`terminald`) that owns PTY session lifecycle and output buffers. A standalone CLI (`vibe-ctl`) provides tmux-like commands (ls, attach, send, resize, stop). The desktop agent becomes a thin client to terminald and continues to serve mobile using the same payload shape, keeping mobile changes minimal. terminald uses WebSocket for all control and streaming; the desktop agent may continue to expose existing HTTP/WS endpoints for mobile compatibility.
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:42:- Provides tmux-like commands and attach/detach UX.
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:77:  "capabilities": ["attach", "poll", "notifications"]
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:104:  "action": "auth|start|list|attach|detach|poll|input|resize|rename|stop|keepalive",
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:126:  "event": "terminal_payload|session_update|heartbeat|stream_paused|stream_resumed|session_warning",
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:155:      "capabilities": ["attach", "poll", "notifications"]
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:162:For session-scoped actions (`attach`, `detach`, `poll`, `input`, `resize`, `rename`, `stop`, `keepalive`), `session_id` is required in the request envelope.
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:181:#### attach
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:186:Server starts streaming `terminal_payload` events for this session. Multiple clients may attach.
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:200:Response data is a `terminal_payload` object (see below).
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:209:{ "data_b64": "AAECAw==", "encoding": "base64" }
/Users/mac/codes/vibe-inspect/docs/terminal_daemon_plan.md:236:### terminal_payload (Compatibility Shape)

thinking
**Planning event send design in terminald**

I'm checking for existing terminal event sending architecture in terminal.rs to reuse or integrate, while noting the need to update activity logging with ralph log and possibly commit integration later.
exec
/bin/zsh -lc "sed -n '1,200p' /Users/mac/codes/vibe-inspect/desktop/src/terminal.rs" in /Users/mac/codes/vibe-inspect succeeded in 18ms:
pub use desktop::terminal_core::{
    handle_terminal_command, list_terminal_sessions, serve_terminal_socket, terminal_manager,
    TerminalActionRequest, TerminalError, TerminalManager, TerminalSessionSummary,
};



thinking
**Designing terminal attach state**

Planning to track per-connection attachment state with fields for last status, exit code, label, and notification sequence to detect changes and decide when to send updates on each tick. The approach favors simple polling every 80ms and sending payloads if output or status changed.
thinking
**Refining streaming attach logic**

Planning to reuse terminal_core's poll_session JSON output parsed for output, notifications, status, exit_code, and label to detect changes per subscription. Attach initializes subscription state with parsed payload fields, updating state on each tick and sending events only when output or status changes. Also considering idempotent attach/detach with session existence checks and updating server capabilities to include "attach", "poll", and "notifications".
thinking
**Planning subscription management and socket refactor**

Detailing a new per-connection subscription state and adapting socket handler to split sender/receiver for concurrent streaming and requests. The design batches subscription polls each tick, sending only changed events and handling detach on errors. Also planning to extend input handling with base64 decoding support and updating send functions to work with the split WebSocket sink.
exec
/bin/zsh -lc 'ls /Users/mac/codes/vibe-inspect/desktop | grep Cargo.lock' in /Users/mac/codes/vibe-inspect succeeded in 19ms:
Cargo.lock


thinking
**Deciding how to update Cargo.lock**

I'm considering adding the base64 dependency to the desktop crate and ensuring Cargo.lock updates by running either `cargo build`, `cargo check`, or `cargo generate-lockfile`; manual Cargo.toml edits plus a lockfile update will keep dependency integrity.
thinking
**Planning base64 decoding and input_bytes addition**

I'm deciding to add the base64 crate to Cargo.toml and update TerminalActionRequest to include optional decoded input_bytes, ensuring invalid base64 triggers a clear error; next, I'll update all TerminalActionRequest initializations accordingly.
exec
/bin/zsh -lc "rg -n \"TerminalActionRequest \\{\" /Users/mac/codes/vibe-inspect/desktop/src -S" in /Users/mac/codes/vibe-inspect succeeded in 29ms:
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:413:pub struct TerminalActionRequest {
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:797:                                    let request = TerminalActionRequest {
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:817:                                    let request = TerminalActionRequest {
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:834:                                let request = TerminalActionRequest {
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:850:                                let request = TerminalActionRequest {
/Users/mac/codes/vibe-inspect/desktop/src/command.rs:128:                let terminal_request = terminal::TerminalActionRequest {
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:350:            let request = TerminalActionRequest {
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:392:            let request = TerminalActionRequest {


thinking
**Planning input and streaming handling**

I'm preparing to update all actions to include `input_bytes: None` without breaking `TerminalActionRequest` usage in desktop code. I'll implement input handling with base64 validation, align terminald streaming to documented response shapes, and ensure attach/detach actions are excluded from the generic command handler to follow protocol and avoid invalid request errors.
thinking
**Defining payload validation and parsing**

I'm confirming payload must be an object or invalid_request is sent, with input payload `None` triggering missing input errors. I'll parse `since` and `notify_since` for attach and poll, storing `notify_since` in subscriptions, and implement base64 decoding with a helper. I'll also prepare to run required tests and add activity logging using the designated logger before committing.
exec
/bin/zsh -lc '/Users/mac/codes/vibe-inspect/ralph log "US-006 start: implementing attach/poll/input streaming + compatibility payloads"' in /Users/mac/codes/vibe-inspect exited 127 in 12ms:
zsh:1: no such file or directory: /Users/mac/codes/vibe-inspect/ralph

thinking
**Checking ralph log path**
exec
/bin/zsh -lc 'ls -l /Users/mac/codes/vibe-inspect/ralph' in /Users/mac/codes/vibe-inspect exited 1 in 17ms:
ls: /Users/mac/codes/vibe-inspect/ralph: No such file or directory

thinking
**Checking for ralph in system path**
exec
/bin/zsh -lc 'which ralph' in /Users/mac/codes/vibe-inspect succeeded in 21ms:
/opt/homebrew/bin/ralph

thinking
**Confirming correct ralph log usage**
exec
/bin/zsh -lc 'ralph log "US-006 start: implementing attach/poll/input streaming + compatibility payloads"' in /Users/mac/codes/vibe-inspect succeeded in 75ms:
Using local templates: /Users/mac/codes/vibe-inspect/.agents/ralph

thinking
**Implementing terminal_core input_bytes field**

I'm adding an optional input_bytes field to TerminalActionRequest and updating all constructors to set it to None initially, preparing terminal_core for input byte handling.
file update
M /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs
@@ -417,2 +417,3 @@
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
apply_patch(auto_approved=true) exited 0 in 22ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs
file update:
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..aa943418a944402908211e8130a3b6ab69e09171
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,

file update:
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..aa943418a944402908211e8130a3b6ab69e09171
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,


thinking
**Updating instantiations with input_bytes**

I'm updating all instances of the terminal stream creation in terminal_core by adding `input_bytes: None` and using search-and-patch to replace four occurrences. Next, I'll open the first snippet to apply and verify the edit.
exec
/bin/zsh -lc "sed -n '770,860p' /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs" in /Users/mac/codes/vibe-inspect succeeded in 17ms:
                    now_ts(),
                    Some(label.as_str()),
                    notification_seq,
                    notifications,
                );
                if sender.send(Message::Text(payload.to_string().into())).await.is_err() {
                    break;
                }
                last_sent_at = Instant::now();

                if status.is_ended() {
                    break;
                }
            }
            maybe_message = receiver.next() => {
                let Some(message) = maybe_message else {
                    break;
                };
                match message {
                    Ok(Message::Text(text)) => {
file update:
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..aa943418a944402908211e8130a3b6ab69e09171
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,

exec
/bin/zsh -lc "sed -n '860,940p' /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs" in /Users/mac/codes/vibe-inspect succeeded in 21ms:
                                    notify_since: None,
                                    working_dir: None,
                                    env: None,
                                };
                                let _ = handle_terminal_command(request);
                            }
                            _ => {}
                        }
                    }
                    Ok(Message::Close(_)) => break,
                    Ok(Message::Ping(payload)) => {
                        let _ = sender.send(Message::Pong(payload)).await;
                    }
                    _ => {}
                }
            }
        }
    }

    Ok(())
file update:
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..aa943418a944402908211e8130a3b6ab69e09171
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,


thinking
**Adding input_bytes None to requests**
file update
M /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs
@@ -802,2 +802,3 @@
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
@@ -822,2 +823,3 @@
                                         input: None,
+                                        input_bytes: None,
                                         cols,
@@ -839,2 +841,3 @@
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
@@ -855,2 +858,3 @@
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
apply_patch(auto_approved=true) exited 0 in 35ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs
file update:
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..158833aa8a377f36a711d01182a969b6dc8faf87
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,

file update:
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..158833aa8a377f36a711d01182a969b6dc8faf87
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,


thinking
**Updating TerminalActionRequest usage**

I'm identifying all instances of TerminalActionRequest across modules, starting with terminal_core's start_session function, and preparing to update its fields accordingly, beginning with patches in desktop/src/command.rs.
exec
/bin/zsh -lc "sed -n '110,160p' /Users/mac/codes/vibe-inspect/desktop/src/command.rs" in /Users/mac/codes/vibe-inspect succeeded in 25ms:
                        }),
                    ),
                    Err(message) => error_response(
                        &request.request_id,
                        "api_request_failed",
                        message,
                        Some(json!({ "request": request_payload })),
                    ),
                }
            }
            Err(response) => response,
        },
        "terminal" => match parse_payload::<TerminalCommandPayload>(
            request.payload,
            "terminal",
            &request.request_id,
        ) {
            Ok(payload) => {
                let terminal_request = terminal::TerminalActionRequest {
                    action: resolve_terminal_action(&payload).to_lowercase(),
file update:
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..158833aa8a377f36a711d01182a969b6dc8faf87
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,


thinking
**Adding None as input_bytes in terminal_request**
file update
M /Users/mac/codes/vibe-inspect/desktop/src/command.rs
@@ -132,2 +132,3 @@
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
apply_patch(auto_approved=true) exited 0 in 35ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/src/command.rs
file update:
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..158833aa8a377f36a711d01182a969b6dc8faf87
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,

file update:
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..158833aa8a377f36a711d01182a969b6dc8faf87
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,


thinking
**Updating input handling for bytes**
exec
/bin/zsh -lc "sed -n '1040,1115p' /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs" in /Users/mac/codes/vibe-inspect succeeded in 24ms:
    } else {
        None
    };
    Ok(build_session_payload(
        "poll",
        &session.id,
        session.status,
        session.next_seq,
        output,
        first_seq,
        truncated,
        snapshot,
        session.exit_code,
        session.last_activity,
        Some(session.label.as_str()),
        session.notification_seq,
        notifications,
    ))
}

file update:
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..158833aa8a377f36a711d01182a969b6dc8faf87
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,


thinking
**Adjusting input handling for conflicts**

I'm updating to unify input_bytes from either input string or input_bytes, returning an error if both or neither are provided, ensuring safe ownership and clear error messaging before writing to the session.
file update
M /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs
@@ -1061,5 +1061,18 @@
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
@@ -1084,3 +1097,3 @@
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
apply_patch(auto_approved=true) exited 0 in 49ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs
file update:
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer

file update:
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer



thinking
**Planning terminald input handling**

I'm deciding to extend TerminalActionRequest in terminald to include input_bytes by base64 decoding input, but will update all instantiations later with attach/poll. Considering adding an AttachmentState struct to track terminal session state like last sequence and exit status for attach/poll management.
thinking
**Designing payload summary and socket helpers**

I'm defining PayloadSummary to encapsulate terminal payload details for efficient diffing and deciding when to send updates, plus helper functions to handle JSON messaging over split WebSocket streams. This lays groundwork for robust attach, poll, and input handling with proper state tracking and error management.
file update
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
@@ -11,3 +11,3 @@
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
@@ -22,4 +22,4 @@
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
apply_patch(auto_approved=true) exited 0 in 45ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..68f3de1f0a967a843213979c9e033b8a917c263a
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -9,7 +9,7 @@
     Router,
 };
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +20,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer

file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..68f3de1f0a967a843213979c9e033b8a917c263a
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -9,7 +9,7 @@
     Router,
 };
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +20,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Adding type alias and subscription structs**

I'm defining a type alias for the WebSocket sender and creating structs for attachment state and payload summary, to insert after the TerminaldState definition.
file update
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
@@ -43,2 +43,25 @@
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
apply_patch(auto_approved=true) exited 0 in 66ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..31a90976e6bd4b794d30266ae6269bfa5754350f
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -9,7 +9,7 @@
     Router,
 };
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +20,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +41,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer

file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..31a90976e6bd4b794d30266ae6269bfa5754350f
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -9,7 +9,7 @@
     Router,
 };
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +20,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +41,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Updating main capabilities list**
file update
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
@@ -73,3 +73,7 @@
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
apply_patch(auto_approved=true) exited 0 in 295ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..b7a8a4f73f530ee3007b043421035bdbe262c6b5
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -9,7 +9,7 @@
     Router,
 };
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +20,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +41,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +71,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer

file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..b7a8a4f73f530ee3007b043421035bdbe262c6b5
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -9,7 +9,7 @@
     Router,
 };
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +20,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +41,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +71,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Updating socket handling call**

I'm changing the call to pass the socket by value instead of mutable reference when invoking the authenticated socket runner.
file update
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
@@ -282,16 +282,54 @@
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
@@ -300,3 +338,8 @@
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
@@ -304,4 +347,5 @@
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
@@ -311,10 +355,5 @@
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
@@ -327,3 +366,3 @@
         send_error_response(
-            socket,
+            sender,
             request_id,
@@ -333,3 +372,4 @@
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
@@ -342,4 +382,5 @@
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
@@ -352,3 +393,3 @@
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
@@ -358,3 +399,4 @@
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
@@ -373,3 +415,4 @@
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
@@ -384,2 +427,3 @@
                 input: None,
+                input_bytes: None,
                 cols: payload
@@ -412,7 +456,173 @@
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
@@ -426,2 +636,3 @@
                 input: None,
+                input_bytes: None,
                 cols: payload
@@ -441,5 +652,6 @@
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
@@ -447,3 +659,3 @@
             send_error_response(
-                socket,
+                sender,
                 request_id,
@@ -453,11 +665,14 @@
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
 }
 
-async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
-    socket.send(Message::Text(value.to_string().into())).await
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
@@ -468,3 +683,3 @@
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
@@ -472,3 +687,3 @@
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
@@ -487,7 +702,7 @@
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
 }
apply_patch(auto_approved=true) exited 0 in 61ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..337c9df90d60cdf2e092f84ce05fce27344b4592
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -9,7 +9,7 @@
     Router,
 };
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +20,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +41,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +71,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +280,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +364,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +380,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +391,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +413,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +425,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +454,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
             }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +634,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +650,42 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
 }
 
-async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
-    socket.send(Message::Text(value.to_string().into())).await
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +700,11 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer

file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..337c9df90d60cdf2e092f84ce05fce27344b4592
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -9,7 +9,7 @@
     Router,
 };
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +20,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +41,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +71,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +280,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +364,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +380,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +391,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +413,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +425,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +454,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
             }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +634,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +650,42 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
 }
 
-async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
-    socket.send(Message::Text(value.to_string().into())).await
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +700,11 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Verifying continued presence of core send_json method**

I'm confirming that the send_json function remains defined for pre-auth use after earlier removal of its WebSocket-specific version and checking that dependent functions like send_error_and_close still call it correctly.
exec
/bin/zsh -lc "sed -n '520,760p' /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs" in /Users/mac/codes/vibe-inspect succeeded in 23ms:
                Ok(session_id) => session_id,
                Err(error) => {
                    send_terminal_error(sender, request_id, error).await;
                    *last_sent_at = Instant::now();
                    return true;
                }
            };
            subscriptions.remove(&session_id);
            send_ok_response(sender, request_id, json!({ "detached": true })).await;
            *last_sent_at = Instant::now();
        }
        "poll" => {
            let session_id = match ensure_session_id(session_id) {
                Ok(session_id) => session_id,
                Err(error) => {
                    send_terminal_error(sender, request_id, error).await;
                    *last_sent_at = Instant::now();
                    return true;
                }
            };
file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..337c9df90d60cdf2e092f84ce05fce27344b4592
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -9,7 +9,7 @@
     Router,
 };
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +20,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +41,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +71,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +280,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +364,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +380,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +391,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +413,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +425,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +454,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
             }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +634,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +650,42 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
 }
 
-async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
-    socket.send(Message::Text(value.to_string().into())).await
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +700,11 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer



thinking
**Restoring essential helper functions**

The send_json function used by send_error_and_close needs to be redefined for WebSocket handling, along with other new helpers like collect_stream_events and summarize_payload, to prevent compile failures. I'll add these functions in the file before parse_env, right after send_terminal_error, ensuring all new utilities are in place.
thinking
**Implementing outlined helper functions**

I'm creating all planned helpers including send_json, send_event, ensure_session_id, summarize_payload, as_stream_payload, collect_stream_events, and decode_base64 with the described logic and payload handling. These will be added together after send_terminal_error to integrate them before parse_env.
file update
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
@@ -709,2 +709,139 @@
 
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
+}
+
 fn parse_env(
apply_patch(auto_approved=true) exited 0 in 60ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..bd4b87d32c4bb5a9a244bb6e6b42dc79f0a15087
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -9,7 +9,7 @@
     Router,
 };
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +20,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +41,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +71,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +280,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +364,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +380,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +391,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +413,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +425,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,11 +454,177 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
                 action: action.clone(),
@@ -397,6 +634,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +650,42 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
 }
 
-async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
-    socket.send(Message::Text(value.to_string().into())).await
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +700,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
+}
+
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer

file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..bd4b87d32c4bb5a9a244bb6e6b42dc79f0a15087
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -9,7 +9,7 @@
     Router,
 };
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +20,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +41,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +71,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +280,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +364,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +380,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +391,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +413,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +425,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,11 +454,177 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
                 action: action.clone(),
@@ -397,6 +634,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +650,42 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
 }
 
-async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
-    socket.send(Message::Text(value.to_string().into())).await
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +700,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
+}
+
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Adding base64 Engine import**

I'm adding the `base64::Engine` import to support the decode_base64 function's use of the base64 crate.
file update
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
@@ -10,2 +10,3 @@
 };
+use base64::Engine;
 use clap::Parser;
apply_patch(auto_approved=true) exited 0 in 58ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..1e67e27d4f592a74b02446464935277312267ab9
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,42 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
 }
 
-async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
-    socket.send(Message::Text(value.to_string().into())).await
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +701,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer

file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..1e67e27d4f592a74b02446464935277312267ab9
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,42 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
 }
 
-async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
-    socket.send(Message::Text(value.to_string().into())).await
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +701,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Adding send_json patch for WebSocket**
file update
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
@@ -677,2 +677,6 @@
 
+async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
+    socket.send(Message::Text(value.to_string().into())).await
+}
+
 async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
apply_patch(auto_approved=true) exited 0 in 58ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs
file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer

file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Verifying input_bytes usage**

I'm searching for all TerminalActionRequest initializations to confirm input_bytes is included everywhere, ensuring consistency across terminald, terminal_core, and command modules.
exec
/bin/zsh -lc 'rg -n "input_bytes" /Users/mac/codes/vibe-inspect/desktop/src -S' in /Users/mac/codes/vibe-inspect succeeded in 25ms:
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:418:    pub input_bytes: Option<Vec<u8>>,
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:803:                                        input_bytes: None,
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:824:                                        input_bytes: None,
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:842:                                    input_bytes: None,
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:859:                                    input_bytes: None,
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:1062:    let input_bytes = match (request.input, request.input_bytes) {
/Users/mac/codes/vibe-inspect/desktop/src/terminal_core.rs:1098:        .write_all(&input_bytes)
/Users/mac/codes/vibe-inspect/desktop/src/command.rs:133:                    input_bytes: None,
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:429:                input_bytes: None,
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:486:                input_bytes: None,
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:546:                input_bytes: None,
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:596:            let input_bytes = match (data, data_b64) {
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:614:                input_bytes,
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:638:                input_bytes: None,
/Users/mac/codes/vibe-inspect/desktop/src/bin/terminald.rs:803:            input_bytes: None,
file update:
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Adding base64 dependency**

I'm updating Cargo.toml in the desktop directory to add base64 version 0.22 as a new dependency, ensuring TerminalActionRequest uses base64 encoding consistently.
file update
M /Users/mac/codes/vibe-inspect/desktop/Cargo.toml
@@ -34,2 +34,3 @@
 crossterm = "0.29.0"
+base64 = "0.22"
 
apply_patch(auto_approved=true) exited 0 in 56ms:
Success. Updated the following files:
M /Users/mac/codes/vibe-inspect/desktop/Cargo.toml
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer

file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer



thinking
**Considering Cargo.lock update and compile fixes**

I'm weighing the best way to update Cargo.lock, leaning toward running `cargo check -p desktop` despite time cost, to regenerate dependencies properly. Also noting a needed import for base64 and clarifying that `send_event` returning a bool via `.await.is_err()` is handled correctly for breaking socket loops.
thinking
**Confirming event stream handling and sequence logic**

I've verified that event streaming correctly updates sequence numbers and last activity timestamps, with attach properly adding subscriptions and emitting initial events using matching sequence logic. Base64 decoding integration and Cargo.lock updates are pending, with plans to run `cargo generate-lockfile` to refresh dependencies efficiently.
exec
/bin/zsh -lc 'cargo generate-lockfile' in /Users/mac/codes/vibe-inspect/desktop succeeded in 7.50s:
    Updating crates.io index
     Locking 593 packages to latest compatible versions
      Adding core-graphics v0.24.0 (available: v0.25.0)
      Adding directories v5.0.1 (available: v6.0.0)
      Adding enigo v0.1.3 (available: v0.6.1)
      Adding generic-array v0.14.7 (available: v0.14.9)
      Adding hostname v0.3.1 (available: v0.4.2)
      Adding matchit v0.8.4 (available: v0.8.6)
      Adding portable-pty v0.8.1 (available: v0.9.0)
      Adding qrcode v0.13.0 (available: v0.14.1)
      Adding rand v0.8.5 (available: v0.9.2)
      Adding rcgen v0.12.1 (available: v0.14.7)
      Adding reqwest v0.12.28 (available: v0.13.1)
      Adding toml v0.8.2 (available: v0.8.23)
      Adding toml_datetime v0.6.3 (available: v0.6.11)
      Adding toml_edit v0.20.2 (available: v0.20.7)
      Adding tower-http v0.5.2 (available: v0.6.8)
      Adding webkit2gtk v2.0.1 (available: v2.0.2)
      Adding webkit2gtk-sys v2.0.1 (available: v2.0.2)
      Adding windows-sys v0.52.0 (available: v0.61.2)
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer



thinking
**Verifying terminal_core updates**

Confirmed the new field in TerminalActionRequest is consistently handled across terminal_core and terminald files, including imports and constructors, with no missing updates found.
thinking
**Reviewing event handling and subscription logic**

Verified concurrency safety in event collection and subscription updates within the same task, confirming no race conditions. Considered but deferred removing subscriptions on session end, as it’s not critical and repeated events are minimal. Confirmed consistent handling of base64 decoding errors, payload construction, and notification sequencing across terminal_core and terminald.
exec
/bin/zsh -lc 'ralph log "US-006: implemented attach/detach/poll/input handling with streaming subscriptions and base64 input support"' in /Users/mac/codes/vibe-inspect succeeded in 93ms:
Using local templates: /Users/mac/codes/vibe-inspect/.agents/ralph
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Running Flutter test command**

I'm preparing to run the `flutter test` command inside the mobile directory as the first step before running analysis and other tests.
exec
/bin/zsh -lc 'cd /Users/mac/codes/vibe-inspect/mobile && flutter test' in /Users/mac/codes/vibe-inspect succeeded in 5.23s:
00:00 +0: loading /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart                                                                                                                00:01 +0: loading /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart                                                                                                                00:02 +0: loading /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart                                                                                                                00:02 +0: /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart: small view offset stays near zero                                                                                     00:02 +1: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Pairing connects with valid secret                                                                                               00:02 +2: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Pairing connects with valid secret                                                                                               00:02 +2: /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart: local cursor offset small view path                                                                                   
local cursor small view offsets: [Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0)]
00:02 +3: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Pairing connects with valid secret                                                                                               00:02 +3: /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart: local cursor offset original view path                                                                                
local cursor original view offsets: [Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0)]
00:02 +4: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Pairing connects with valid secret                                                                                               00:02 +4: /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart: raw 320x240 remote cursor path                                                                                        
raw 320x240 remote delta at Offset(0.0, 0.0) = Offset(0.0, 0.0)
raw 320x240 remote delta at Offset(40.0, 30.0) = Offset(0.0, 0.0)
raw 320x240 remote delta at Offset(80.0, 60.0) = Offset(0.0, 0.0)
raw 320x240 remote delta at Offset(160.0, 120.0) = Offset(0.0, 0.0)
raw 320x240 remote delta at Offset(240.0, 180.0) = Offset(0.0, 0.0)
raw 320x240 remote delta at Offset(319.0, 239.0) = Offset(0.0, 0.0)
00:02 +5: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Pairing connects with valid secret                                                                                               00:02 +5: /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart: raw 320x240 local cursor path                                                                                         
raw 320x240 local offsets: [Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0)]
00:02 +6: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Pairing connects with valid secret                                                                                               00:02 +7: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Pairing connects with valid secret                                                                                               00:02 +7: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Expired token shows retry prompt                                                                                                 00:02 +8: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Expired token shows retry prompt                                                                                                 00:02 +8: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Tunnel error message is shown after scan                                                                                         00:03 +8: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Tunnel error message is shown after scan                                                                                         00:03 +9: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Tunnel error message is shown after scan                                                                                         00:03 +9: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Storage initialization failure blocks the UI                                                                                     00:03 +10: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Storage initialization failure blocks the UI                                                                                    00:03 +10: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace API session opens API explorer                                                                                        00:03 +11: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace API session opens API explorer                                                                                        00:03 +11: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace terminal session opens session                                                                                        00:03 +11: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace terminal session opens session                                                                                        
[error:terminal_sessions] Desktop agent returned an error response. (code=http_error request_id=1769964491328136-1995417902 endpoint=https://terminal.agent/command details={"status":400})
[error:terminal_poll] Desktop agent returned an error response. (code=http_error request_id=1769964491339248-1068392435 endpoint=https://terminal.agent/command details={"status":400})
00:03 +12: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace terminal session opens session                                                                                        00:03 +12: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: API Explorer blocks invalid JSON body                                                                                           00:03 +13: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: API Explorer blocks invalid JSON body                                                                                           00:03 +13: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace session with missing context shows error state                                                                        00:03 +14: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace session with missing context shows error state                                                                        00:03 +14: All tests passed!                                                                                                                                                                           
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Running static analysis**

Next step is performing static analysis with `flutter analyze` after successful tests.
exec
/bin/zsh -lc 'cd /Users/mac/codes/vibe-inspect/mobile && flutter analyze' in /Users/mac/codes/vibe-inspect exited 1 in 2.58s:
Analyzing mobile...                                             

   info • Unnecessary constructor invocation • lib/main.dart:1966:24 • prefer_collection_literals
   info • 'groupValue' is deprecated and shouldn't be used. Use a RadioGroup ancestor to manage group value instead. This feature was deprecated after v3.32.0-0.0.pre • lib/main.dart:7396:21 • deprecated_member_use
   info • 'onChanged' is deprecated and shouldn't be used. Use RadioGroup to handle value change instead. This feature was deprecated after v3.32.0-0.0.pre • lib/main.dart:7398:21 • deprecated_member_use
   info • Unnecessary braces in a string interpolation • lib/main.dart:10050:39 • unnecessary_brace_in_string_interps
warning • The '!' will have no effect because the receiver can't be null • lib/main.dart:10076:42 • unnecessary_non_null_assertion
warning • The '!' will have no effect because the receiver can't be null • lib/main.dart:10380:41 • unnecessary_non_null_assertion
warning • The value of the field '_dragSlop' isn't used • lib/main.dart:10647:23 • unused_field
warning • The receiver can't be 'null' because of short-circuiting, so the null-aware operator '?.' can't be used • lib/main.dart:12062:36 • invalid_null_aware_operator
warning • The receiver can't be 'null' because of short-circuiting, so the null-aware operator '?.' can't be used • lib/main.dart:12063:38 • invalid_null_aware_operator
warning • The '!' will have no effect because the receiver can't be null • lib/main.dart:12821:18 • unnecessary_non_null_assertion
warning • The '!' will have no effect because the receiver can't be null • lib/main.dart:12826:18 • unnecessary_non_null_assertion
   info • Unnecessary braces in a string interpolation • lib/main.dart:14271:12 • unnecessary_brace_in_string_interps
   info • 'value' is deprecated and shouldn't be used. Use initialValue instead. This will set the initial value for the form field. This feature was deprecated after v3.33.0-1.0.pre • lib/main.dart:15147:17 • deprecated_member_use
   info • Unnecessary braces in a string interpolation • lib/main.dart:15184:31 • unnecessary_brace_in_string_interps
   info • Unnecessary braces in a string interpolation • lib/main.dart:15213:38 • unnecessary_brace_in_string_interps
warning • The value of the field '_cpixel24A' isn't used • lib/vnc_client.dart:84:8 • unused_field
   info • 'translate' is deprecated and shouldn't be used. Use translateByVector3, translateByVector4, or translateByDouble instead • test/vnc_canvas_offset_test.dart:114:27 • deprecated_member_use
   info • 'scale' is deprecated and shouldn't be used. Use scaleByVector3, scaleByVector4, or scaleByDouble instead • test/vnc_canvas_offset_test.dart:115:27 • deprecated_member_use
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Noting analyze failure due to third-party dependencies**

The flutter analyze failure stems from known issues with third_party build_tool dependencies (guardrail) and is expected; moving on to run the flutter tests.
exec
/bin/zsh -lc /Users/mac/codes/vibe-inspect/scripts/flutter_test.sh in /Users/mac/codes/vibe-inspect succeeded in 3.70s:
00:00 +0: loading /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart                                                                                                                00:01 +0: loading /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart                                                                                                                00:01 +0: loading /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart                                                                                                                           00:01 +0: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Pairing connects with valid secret                                                                                               00:01 +1: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Pairing connects with valid secret                                                                                               00:01 +2: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Pairing connects with valid secret                                                                                               00:01 +3: /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart: local cursor offset small view path                                                                                   00:01 +3: /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart: local cursor offset small view path                                                                                   
local cursor small view offsets: [Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0)]
00:01 +4: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Expired token shows retry prompt                                                                                                 00:02 +4: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Expired token shows retry prompt                                                                                                 00:02 +4: /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart: local cursor offset original view path                                                                                
local cursor original view offsets: [Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0)]
00:02 +5: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Expired token shows retry prompt                                                                                                 00:02 +5: /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart: raw 320x240 remote cursor path                                                                                        
raw 320x240 remote delta at Offset(0.0, 0.0) = Offset(0.0, 0.0)
raw 320x240 remote delta at Offset(40.0, 30.0) = Offset(0.0, 0.0)
raw 320x240 remote delta at Offset(80.0, 60.0) = Offset(0.0, 0.0)
raw 320x240 remote delta at Offset(160.0, 120.0) = Offset(0.0, 0.0)
raw 320x240 remote delta at Offset(240.0, 180.0) = Offset(0.0, 0.0)
raw 320x240 remote delta at Offset(319.0, 239.0) = Offset(0.0, 0.0)
00:02 +6: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Expired token shows retry prompt                                                                                                 00:02 +7: /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart: raw 320x240 local cursor path                                                                                         00:02 +7: /Users/mac/codes/vibe-inspect/mobile/test/vnc_canvas_offset_test.dart: raw 320x240 local cursor path                                                                                         
raw 320x240 local offsets: [Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0), Offset(11.0, 11.0)]
00:02 +8: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Tunnel error message is shown after scan                                                                                         00:02 +9: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Tunnel error message is shown after scan                                                                                         00:02 +9: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Storage initialization failure blocks the UI                                                                                     00:02 +10: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Storage initialization failure blocks the UI                                                                                    00:02 +10: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace API session opens API explorer                                                                                        00:02 +11: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace API session opens API explorer                                                                                        00:02 +11: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace terminal session opens session                                                                                        00:02 +11: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace terminal session opens session                                                                                        
[error:terminal_sessions] Desktop agent returned an error response. (code=http_error request_id=1769964506326927-560704297 endpoint=https://terminal.agent/command details={"status":400})
[error:terminal_poll] Desktop agent returned an error response. (code=http_error request_id=1769964506338033-431382018 endpoint=https://terminal.agent/command details={"status":400})
00:02 +12: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace terminal session opens session                                                                                        00:02 +12: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: API Explorer blocks invalid JSON body                                                                                           00:02 +13: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: API Explorer blocks invalid JSON body                                                                                           00:02 +13: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace session with missing context shows error state                                                                        00:02 +14: /Users/mac/codes/vibe-inspect/mobile/test/widget_test.dart: Workspace session with missing context shows error state                                                                        00:02 +14: All tests passed!                                                                                                                                                                           
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Running full Flutter analysis**
exec
/bin/zsh -lc /Users/mac/codes/vibe-inspect/scripts/flutter_analyze.sh in /Users/mac/codes/vibe-inspect exited 1 in 2.44s:
Analyzing mobile...                                             

   info • Unnecessary constructor invocation • lib/main.dart:1966:24 • prefer_collection_literals
   info • 'groupValue' is deprecated and shouldn't be used. Use a RadioGroup ancestor to manage group value instead. This feature was deprecated after v3.32.0-0.0.pre • lib/main.dart:7396:21 • deprecated_member_use
   info • 'onChanged' is deprecated and shouldn't be used. Use RadioGroup to handle value change instead. This feature was deprecated after v3.32.0-0.0.pre • lib/main.dart:7398:21 • deprecated_member_use
   info • Unnecessary braces in a string interpolation • lib/main.dart:10050:39 • unnecessary_brace_in_string_interps
warning • The '!' will have no effect because the receiver can't be null • lib/main.dart:10076:42 • unnecessary_non_null_assertion
warning • The '!' will have no effect because the receiver can't be null • lib/main.dart:10380:41 • unnecessary_non_null_assertion
warning • The value of the field '_dragSlop' isn't used • lib/main.dart:10647:23 • unused_field
warning • The receiver can't be 'null' because of short-circuiting, so the null-aware operator '?.' can't be used • lib/main.dart:12062:36 • invalid_null_aware_operator
warning • The receiver can't be 'null' because of short-circuiting, so the null-aware operator '?.' can't be used • lib/main.dart:12063:38 • invalid_null_aware_operator
warning • The '!' will have no effect because the receiver can't be null • lib/main.dart:12821:18 • unnecessary_non_null_assertion
warning • The '!' will have no effect because the receiver can't be null • lib/main.dart:12826:18 • unnecessary_non_null_assertion
   info • Unnecessary braces in a string interpolation • lib/main.dart:14271:12 • unnecessary_brace_in_string_interps
   info • 'value' is deprecated and shouldn't be used. Use initialValue instead. This will set the initial value for the form field. This feature was deprecated after v3.33.0-1.0.pre • lib/main.dart:15147:17 • deprecated_member_use
   info • Unnecessary braces in a string interpolation • lib/main.dart:15184:31 • unnecessary_brace_in_string_interps
   info • Unnecessary braces in a string interpolation • lib/main.dart:15213:38 • unnecessary_brace_in_string_interps
warning • The value of the field '_cpixel24A' isn't used • lib/vnc_client.dart:84:8 • unused_field
   info • 'translate' is deprecated and shouldn't be used. Use translateByVector3, translateByVector4, or translateByDouble instead • test/vnc_canvas_offset_test.dart:114:27 • deprecated_member_use
   info • 'scale' is deprecated and shouldn't be used. Use scaleByVector3, scaleByVector4, or scaleByDouble instead • test/vnc_canvas_offset_test.dart:115:27 • deprecated_member_use
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Confirming need to add logging after tests**
exec
/bin/zsh -lc 'ralph log "US-006: ran flutter tests/analyze (analyze failed on known third_party/flutter_quic build_tool deps)"' in /Users/mac/codes/vibe-inspect succeeded in 92ms:
Using local templates: /Users/mac/codes/vibe-inspect/.agents/ralph
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Checking git status before commit**
exec
/bin/zsh -lc 'git status -sb' in /Users/mac/codes/vibe-inspect succeeded in 38ms:
## main
 M .agents/tasks/prd-terminal-daemon.json
 M .ralph/activity.log
 M .ralph/errors.log
 M .ralph/runs/run-20260201-231810-31541-iter-5.log
 M desktop/Cargo.lock
 M desktop/Cargo.toml
 M desktop/src/bin/terminald.rs
 M desktop/src/command.rs
 M desktop/src/terminal_core.rs
?? .ralph/.tmp/prompt-20260201-231810-31541-6.md
?? .ralph/.tmp/story-20260201-231810-31541-6.json
?? .ralph/.tmp/story-20260201-231810-31541-6.md
?? .ralph/runs/run-20260201-231810-31541-iter-5.md
?? .ralph/runs/run-20260201-231810-31541-iter-6.log
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer



thinking
**Committing all changes including system-modified files**

All changes made during the run, including updates to logs and system-modified files like the prd-terminal-daemon.json, must be committed without reverting; I'll stage everything to comply with this requirement.
thinking
**Reviewing code and logs before commit**

I’m preparing to run a security, performance, and regression review by inspecting diffs of key files like terminald.rs and confirming progress and error logs are up to date; no new errors require logging beyond what’s documented.
exec
/bin/zsh -lc 'git diff --stat' in /Users/mac/codes/vibe-inspect succeeded in 51ms:
 .agents/tasks/prd-terminal-daemon.json           |   13 +-
 .ralph/activity.log                              |    6 +
 .ralph/errors.log                                |    1 +
 .ralph/runs/run-20260201-231810-31541-iter-5.log | 1156 +++++++++++++++++++++-
 desktop/Cargo.lock                               |    7 +-
 desktop/Cargo.toml                               |    1 +
 desktop/src/bin/terminald.rs                     |  470 ++++++++-
 desktop/src/command.rs                           |    1 +
 desktop/src/terminal_core.rs                     |   26 +-
 9 files changed, 1625 insertions(+), 56 deletions(-)
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Preparing to use commit skill**

I'm proceeding to open the local commit skill file to follow its instructions for committing all changes as requested.
exec
/bin/zsh -lc 'cat /Users/mac/codes/vibe-inspect/.codex/skills/commit/SKILL.md' in /Users/mac/codes/vibe-inspect succeeded in 20ms:
---
name: commit
description: Write conventional commit messages with type, scope, and subject when the user wants to commit changes or save work.
---

# Git Commit

Creates git commits following Conventional Commits format with proper type, scope, and subject.

## Quick Start

```bash
# 1. Stage changes
git add <files>  # or: git add -A

# 2. Create commit (branch commit format)
git commit -m "type(scope): subject

Body explaining HOW and WHY.
Reference: Task X.Y, Req N"
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Performing mental security and regression review**

I'm confirming no security vulnerabilities or regressions with the base64 decode, polling frequency, and new terminal actions, noting possible performance impact if many sessions but acceptable. Next, I'll stage all changes for commit.
exec
/bin/zsh -lc 'git add -A' in /Users/mac/codes/vibe-inspect succeeded in 70ms:

file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Crafting conventional commit message**

Preparing a concise commit message with scope and body referencing US-006, emphasizing additions to terminald's attach/poll/input streaming and base64 input handling while keeping line lengths within limits.
exec
/bin/zsh -lc 'git commit -m "feat(terminald): add attach poll input

Implement WS subscriptions for attach/detach with streaming events,
add poll/input handling, and support base64 input bytes through the
terminal core.

Reference: US-006"' in /Users/mac/codes/vibe-inspect succeeded in 57ms:
[main cafd50f] feat(terminald): add attach poll input
 Committer: mac <mac@SilvanMacBook-Pro.local>
Your name and email address were configured automatically based
on your username and hostname. Please check that they are accurate.
You can suppress this message by setting them explicitly. Run the
following command and follow the instructions in your editor to edit
your configuration file:

    git config --global --edit

After doing this, you may fix the identity used for this commit with:

    git commit --amend --reset-author

 14 files changed, 20742 insertions(+), 56 deletions(-)
 create mode 100644 .ralph/.tmp/prompt-20260201-231810-31541-6.md
 create mode 100644 .ralph/.tmp/story-20260201-231810-31541-6.json
 create mode 100644 .ralph/.tmp/story-20260201-231810-31541-6.md
 create mode 100644 .ralph/runs/run-20260201-231810-31541-iter-5.md
 create mode 100644 .ralph/runs/run-20260201-231810-31541-iter-6.log
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Verifying clean git status**
exec
/bin/zsh -lc 'git status --porcelain' in /Users/mac/codes/vibe-inspect succeeded in 39ms:
 M .ralph/runs/run-20260201-231810-31541-iter-6.log
file update:
diff --git a/desktop/Cargo.toml b/desktop/Cargo.toml
index e8641f5523105ff28eff24dc6db8cff72eb06fdd..2b75ca1a7c0925907d988643b8323e528b08f0be
--- a/desktop/Cargo.toml
+++ b/desktop/Cargo.toml
@@ -32,6 +32,7 @@
 clap = { version = "4.5.56", features = ["derive"] }
 tokio-tungstenite = "0.28.0"
 crossterm = "0.29.0"
+base64 = "0.22"
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-graphics = "0.24"
diff --git a/desktop/src/bin/terminald.rs b/desktop/src/bin/terminald.rs
index 18324de775362b54283e8b08b9136e43f6462161..65897a86a75d39f5c65491655d3332c45217973d
--- a/desktop/src/bin/terminald.rs
+++ b/desktop/src/bin/terminald.rs
@@ -8,8 +8,9 @@
     routing::get,
     Router,
 };
+use base64::Engine;
 use clap::Parser;
-use futures_util::{SinkExt, StreamExt};
+use futures_util::{stream::SplitSink, SinkExt, StreamExt};
 use rand::{distributions::Alphanumeric, Rng};
 
 use desktop::terminal_core::{
@@ -20,8 +21,8 @@
 use serde_json::{json, Value};
 use std::collections::HashMap;
 use std::net::SocketAddr;
-use std::time::{Duration, SystemTime, UNIX_EPOCH};
-use tokio::time::timeout;
+use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
+use tokio::time::{interval, timeout, MissedTickBehavior};
 
 #[derive(Debug, Parser)]
 #[command(name = "terminald", about = "Terminal daemon for Vibe", version)]
@@ -41,6 +42,29 @@
     capabilities: Vec<String>,
 }
 
+type WsSender = SplitSink<WebSocket, Message>;
+
+#[derive(Debug, Clone)]
+struct AttachmentState {
+    last_seq: u64,
+    last_notification_seq: u64,
+    last_status: String,
+    last_exit_code: Option<i64>,
+    last_label: String,
+}
+
+#[derive(Debug, Clone)]
+struct PayloadSummary {
+    next_seq: u64,
+    notification_next_seq: u64,
+    status: String,
+    exit_code: Option<i64>,
+    label: String,
+    output_len: usize,
+    notifications_len: usize,
+    truncated: bool,
+}
+
 #[tokio::main]
 async fn main() {
     let args = TerminaldArgs::parse();
@@ -48,7 +72,11 @@
     let bind = args.bind.trim().to_string();
     let ws_url = format!("ws://{bind}{ws_path}");
     let token = random_token(32);
-    let capabilities: Vec<String> = Vec::new();
+    let capabilities: Vec<String> = vec![
+        "attach".to_string(),
+        "poll".to_string(),
+        "notifications".to_string(),
+    ];
     let discovery = TerminalDiscoveryFile {
         ws_url,
         token: token.clone(),
@@ -253,43 +281,82 @@
         return;
     }
 
-    run_authenticated_socket(&mut socket).await;
+    run_authenticated_socket(socket).await;
 }
 
-async fn run_authenticated_socket(socket: &mut WebSocket) {
-    while let Some(message) = socket.recv().await {
-        match message {
-            Ok(Message::Ping(payload)) => {
-                let _ = socket.send(Message::Pong(payload)).await;
+async fn run_authenticated_socket(socket: WebSocket) {
+    let (mut sender, mut receiver) = socket.split();
+    let mut subscriptions: HashMap<String, AttachmentState> = HashMap::new();
+    let mut tick = interval(Duration::from_millis(80));
+    tick.set_missed_tick_behavior(MissedTickBehavior::Skip);
+    let mut last_sent_at = Instant::now();
+
+    loop {
+        tokio::select! {
+            _ = tick.tick() => {
+                if !subscriptions.is_empty() {
+                    let (events, detached) = collect_stream_events(&mut subscriptions);
+                    for event in events {
+                        if send_json_sender(&mut sender, event).await.is_err() {
+                            return;
+                        }
+                        last_sent_at = Instant::now();
+                    }
+                    for session_id in detached {
+                        subscriptions.remove(&session_id);
+                    }
+                }
+
+                if last_sent_at.elapsed() > Duration::from_secs(15) {
+                    if sender.send(Message::Ping(Vec::new().into())).await.is_err() {
+                        return;
+                    }
+                    last_sent_at = Instant::now();
+                }
             }
-            Ok(Message::Text(text)) => {
-                handle_request(socket, &text).await;
+            maybe_message = receiver.next() => {
+                let Some(message) = maybe_message else {
+                    break;
+                };
+                match message {
+                    Ok(Message::Ping(payload)) => {
+                        let _ = sender.send(Message::Pong(payload)).await;
+                        last_sent_at = Instant::now();
+                    }
+                    Ok(Message::Text(text)) => {
+                        if handle_request(&mut sender, &mut subscriptions, &text, &mut last_sent_at).await {
+                            continue;
+                        }
+                        break;
+                    }
+                    Ok(Message::Close(_)) | Err(_) => break,
+                    _ => {}
+                }
             }
-            Ok(Message::Close(_)) | Err(_) => break,
-            _ => {}
         }
     }
 }
 
-async fn handle_request(socket: &mut WebSocket, text: &str) {
+async fn handle_request(
+    sender: &mut WsSender,
+    subscriptions: &mut HashMap<String, AttachmentState>,
+    text: &str,
+    last_sent_at: &mut Instant,
+) -> bool {
     let parsed: Value = match serde_json::from_str(text) {
         Ok(value) => value,
         Err(_) => {
-            send_error_response(socket, "", "invalid_request", "Invalid JSON payload.").await;
-            return;
+            send_error_response(sender, "", "invalid_request", "Invalid JSON payload.").await;
+            *last_sent_at = Instant::now();
+            return true;
         }
     };
 
     let request_id = parsed.get("id").and_then(|value| value.as_str()).unwrap_or("");
     if request_id.trim().is_empty() {
-        send_error_response(
-            socket,
-            "",
-            "invalid_request",
-            "Missing request id.",
-        )
-        .await;
-        return;
+        send_error_response(sender, "", "invalid_request", "Missing request id.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let message_type = parsed
@@ -298,13 +365,14 @@
         .unwrap_or("");
     if message_type != "req" {
         send_error_response(
-            socket,
+            sender,
             request_id,
             "invalid_request",
             "Unsupported message type.",
         )
         .await;
-        return;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let action = parsed
@@ -313,8 +381,9 @@
         .unwrap_or("")
         .to_lowercase();
     if action.is_empty() {
-        send_error_response(socket, request_id, "invalid_request", "Missing action.").await;
-        return;
+        send_error_response(sender, request_id, "invalid_request", "Missing action.").await;
+        *last_sent_at = Instant::now();
+        return true;
     }
 
     let payload = match parsed.get("payload") {
@@ -323,13 +392,14 @@
                 Some(map)
             } else {
                 send_error_response(
-                    socket,
+                    sender,
                     request_id,
                     "invalid_request",
                     "Payload must be an object.",
                 )
                 .await;
-                return;
+                *last_sent_at = Instant::now();
+                return true;
             }
         }
         None => None,
@@ -344,7 +414,8 @@
         "list" => {
             let sessions = list_terminal_sessions();
             let data = json!({ "sessions": sessions });
-            send_ok_response(socket, request_id, data).await;
+            send_ok_response(sender, request_id, data).await;
+            *last_sent_at = Instant::now();
         }
         "start" => {
             let request = TerminalActionRequest {
@@ -355,6 +426,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -383,10 +455,176 @@
                         "session_id": session_id,
                         "payload": payload,
                     });
-                    send_ok_response(socket, request_id, data).await;
+                    send_ok_response(sender, request_id, data).await;
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "attach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let since = payload
+                .and_then(|payload| payload.get("since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let notify_since = payload
+                .and_then(|payload| payload.get("notify_since"))
+                .and_then(|value| value.as_u64())
+                .unwrap_or(0);
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id.clone()),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: Some(since),
+                limit: None,
+                notify_since: Some(notify_since),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => {
+                    let summary = summarize_payload(&payload);
+                    let attachment = AttachmentState {
+                        last_seq: summary.next_seq,
+                        last_notification_seq: summary.notification_next_seq,
+                        last_status: summary.status.clone(),
+                        last_exit_code: summary.exit_code,
+                        last_label: summary.label.clone(),
+                    };
+                    subscriptions.insert(session_id.clone(), attachment);
+                    send_ok_response(sender, request_id, json!({ "attached": true })).await;
+                    let stream_payload = as_stream_payload(payload);
+                    if send_event(sender, "terminal_payload", &session_id, stream_payload)
+                        .await
+                        .is_err()
+                    {
+                        return false;
+                    }
+                }
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "detach" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            subscriptions.remove(&session_id);
+            send_ok_response(sender, request_id, json!({ "detached": true })).await;
+            *last_sent_at = Instant::now();
+        }
+        "poll" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
+                }
+            };
+            let request = TerminalActionRequest {
+                action: "poll".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes: None,
+                cols: None,
+                rows: None,
+                since: payload
+                    .and_then(|payload| payload.get("since"))
+                    .and_then(|value| value.as_u64()),
+                limit: payload
+                    .and_then(|payload| payload.get("limit"))
+                    .and_then(|value| value.as_u64())
+                    .and_then(|value| usize::try_from(value).ok()),
+                notify_since: payload
+                    .and_then(|payload| payload.get("notify_since"))
+                    .and_then(|value| value.as_u64()),
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
+            }
+            *last_sent_at = Instant::now();
+        }
+        "input" => {
+            let session_id = match ensure_session_id(session_id) {
+                Ok(session_id) => session_id,
+                Err(error) => {
+                    send_terminal_error(sender, request_id, error).await;
+                    *last_sent_at = Instant::now();
+                    return true;
                 }
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+            };
+            let data = payload
+                .and_then(|payload| payload.get("data"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            let data_b64 = payload
+                .and_then(|payload| payload.get("data_b64"))
+                .and_then(|value| value.as_str())
+                .map(|value| value.to_string());
+            if data.is_some() && data_b64.is_some() {
+                send_error_response(
+                    sender,
+                    request_id,
+                    "invalid_request",
+                    "Provide either data or data_b64, not both.",
+                )
+                .await;
+                *last_sent_at = Instant::now();
+                return true;
+            }
+            let input_bytes = match (data, data_b64) {
+                (Some(data), None) => Some(data.into_bytes()),
+                (None, Some(encoded)) => match decode_base64(&encoded) {
+                    Ok(bytes) => Some(bytes),
+                    Err(message) => {
+                        send_error_response(sender, request_id, "invalid_request", &message).await;
+                        *last_sent_at = Instant::now();
+                        return true;
+                    }
+                },
+                (None, None) => None,
+                _ => None,
+            };
+            let request = TerminalActionRequest {
+                action: "input".to_string(),
+                session_id: Some(session_id),
+                label: None,
+                input: None,
+                input_bytes,
+                cols: None,
+                rows: None,
+                since: None,
+                limit: None,
+                notify_since: None,
+                working_dir: None,
+                env: None,
+            };
+            match handle_terminal_command(request) {
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         "stop" | "kill" | "rename" | "resize" | "keepalive" => {
             let request = TerminalActionRequest {
@@ -397,6 +635,7 @@
                     .and_then(|value| value.as_str())
                     .map(|value| value.to_string()),
                 input: None,
+                input_bytes: None,
                 cols: payload
                     .and_then(|payload| payload.get("cols"))
                     .and_then(|value| value.as_u64())
@@ -412,38 +651,46 @@
                 env: None,
             };
             match handle_terminal_command(request) {
-                Ok(payload) => send_ok_response(socket, request_id, payload).await,
-                Err(error) => send_terminal_error(socket, request_id, error).await,
+                Ok(payload) => send_ok_response(sender, request_id, payload).await,
+                Err(error) => send_terminal_error(sender, request_id, error).await,
             }
+            *last_sent_at = Instant::now();
         }
         _ => {
             send_error_response(
-                socket,
+                sender,
                 request_id,
                 "unsupported_action",
                 "Unsupported action.",
             )
             .await;
+            *last_sent_at = Instant::now();
         }
     }
+
+    true
+}
+
+async fn send_json_sender(sender: &mut WsSender, value: Value) -> Result<(), axum::Error> {
+    sender.send(Message::Text(value.to_string().into())).await
 }
 
 async fn send_json(socket: &mut WebSocket, value: Value) -> Result<(), axum::Error> {
     socket.send(Message::Text(value.to_string().into())).await
 }
 
-async fn send_ok_response(socket: &mut WebSocket, request_id: &str, data: Value) {
+async fn send_ok_response(sender: &mut WsSender, request_id: &str, data: Value) {
     let payload = json!({
         "type": "res",
         "id": request_id,
         "ok": true,
         "data": data,
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
 async fn send_error_response(
-    socket: &mut WebSocket,
+    sender: &mut WsSender,
     request_id: &str,
     code: &str,
     message: &str,
@@ -458,11 +705,148 @@
             "message": message,
         }
     });
-    let _ = send_json(socket, payload).await;
+    let _ = send_json_sender(sender, payload).await;
 }
 
-async fn send_terminal_error(socket: &mut WebSocket, request_id: &str, error: TerminalError) {
-    send_error_response(socket, request_id, error.code, &error.message).await;
+async fn send_terminal_error(sender: &mut WsSender, request_id: &str, error: TerminalError) {
+    send_error_response(sender, request_id, error.code, &error.message).await;
+}
+
+async fn send_event(
+    sender: &mut WsSender,
+    event: &str,
+    session_id: &str,
+    data: Value,
+) -> Result<(), axum::Error> {
+    let payload = json!({
+        "type": "event",
+        "event": event,
+        "session_id": session_id,
+        "data": data,
+    });
+    send_json_sender(sender, payload).await
+}
+
+fn ensure_session_id(session_id: Option<String>) -> Result<String, TerminalError> {
+    let session_id = session_id
+        .ok_or_else(|| TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        })?;
+    let trimmed = session_id.trim();
+    if trimmed.is_empty() {
+        return Err(TerminalError {
+            code: "missing_session",
+            message: "Missing session id.".to_string(),
+        });
+    }
+    Ok(trimmed.to_string())
+}
+
+fn summarize_payload(payload: &Value) -> PayloadSummary {
+    PayloadSummary {
+        next_seq: payload
+            .get("next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        notification_next_seq: payload
+            .get("notification_next_seq")
+            .and_then(|value| value.as_u64())
+            .unwrap_or(0),
+        status: payload
+            .get("status")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        exit_code: payload.get("exit_code").and_then(|value| value.as_i64()),
+        label: payload
+            .get("label")
+            .and_then(|value| value.as_str())
+            .unwrap_or("")
+            .to_string(),
+        output_len: payload
+            .get("output")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        notifications_len: payload
+            .get("notifications")
+            .and_then(|value| value.as_array())
+            .map(|value| value.len())
+            .unwrap_or(0),
+        truncated: payload
+            .get("truncated")
+            .and_then(|value| value.as_bool())
+            .unwrap_or(false),
+    }
+}
+
+fn as_stream_payload(mut payload: Value) -> Value {
+    if let Some(map) = payload.as_object_mut() {
+        map.insert("action".to_string(), json!("stream"));
+    }
+    payload
+}
+
+fn collect_stream_events(
+    subscriptions: &mut HashMap<String, AttachmentState>,
+) -> (Vec<Value>, Vec<String>) {
+    let mut events = Vec::new();
+    let mut detached = Vec::new();
+
+    for (session_id, state) in subscriptions.iter_mut() {
+        let request = TerminalActionRequest {
+            action: "poll".to_string(),
+            session_id: Some(session_id.clone()),
+            label: None,
+            input: None,
+            input_bytes: None,
+            cols: None,
+            rows: None,
+            since: Some(state.last_seq),
+            limit: None,
+            notify_since: Some(state.last_notification_seq),
+            working_dir: None,
+            env: None,
+        };
+        match handle_terminal_command(request) {
+            Ok(payload) => {
+                let summary = summarize_payload(&payload);
+                let seq_changed = summary.next_seq != state.last_seq
+                    || summary.notification_next_seq != state.last_notification_seq;
+                let should_send = summary.output_len > 0
+                    || summary.notifications_len > 0
+                    || summary.truncated
+                    || summary.status != state.last_status
+                    || summary.exit_code != state.last_exit_code
+                    || summary.label != state.last_label
+                    || seq_changed;
+                if should_send {
+                    state.last_seq = summary.next_seq;
+                    state.last_notification_seq = summary.notification_next_seq;
+                    state.last_status = summary.status;
+                    state.last_exit_code = summary.exit_code;
+                    state.last_label = summary.label;
+                    let stream_payload = as_stream_payload(payload);
+                    events.push(json!({
+                        "type": "event",
+                        "event": "terminal_payload",
+                        "session_id": session_id,
+                        "data": stream_payload,
+                    }));
+                }
+            }
+            Err(_) => detached.push(session_id.clone()),
+        }
+    }
+
+    (events, detached)
+}
+
+fn decode_base64(value: &str) -> Result<Vec<u8>, String> {
+    base64::engine::general_purpose::STANDARD
+        .decode(value.as_bytes())
+        .map_err(|_| "Invalid base64 input.".to_string())
 }
 
 fn parse_env(
diff --git a/desktop/src/command.rs b/desktop/src/command.rs
index d726c0c848eb755ccb95fd62b1d081ccc13279a1..31c65192f0931687ef8f37759b943cde7981fff6
--- a/desktop/src/command.rs
+++ b/desktop/src/command.rs
@@ -130,6 +130,7 @@
                     session_id: payload.session_id.clone(),
                     label: payload.label.clone(),
                     input: resolve_terminal_input(&payload),
+                    input_bytes: None,
                     cols: payload.cols,
                     rows: payload.rows,
                     since: payload.since,
diff --git a/desktop/src/terminal_core.rs b/desktop/src/terminal_core.rs
index 1c9ac7f43a256d1478d2ad55c68dd24364d6dddc..d6c5574679362c1121b10355ed42614dedd2d522
--- a/desktop/src/terminal_core.rs
+++ b/desktop/src/terminal_core.rs
@@ -415,6 +415,7 @@
     pub session_id: Option<String>,
     pub label: Option<String>,
     pub input: Option<String>,
+    pub input_bytes: Option<Vec<u8>>,
     pub cols: Option<u16>,
     pub rows: Option<u16>,
     pub since: Option<u64>,
@@ -799,6 +800,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: Some(input.to_string()),
+                                        input_bytes: None,
                                         cols: None,
                                         rows: None,
                                         since: None,
@@ -819,6 +821,7 @@
                                         session_id: Some(session_id.clone()),
                                         label: None,
                                         input: None,
+                                        input_bytes: None,
                                         cols,
                                         rows,
                                         since: None,
@@ -836,6 +839,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -852,6 +856,7 @@
                                     session_id: Some(session_id.clone()),
                                     label: None,
                                     input: None,
+                                    input_bytes: None,
                                     cols: None,
                                     rows: None,
                                     since: None,
@@ -1054,9 +1059,22 @@
 
 fn input_session(request: TerminalActionRequest) -> Result<Value, TerminalError> {
     let session_id = required_session_id(request.session_id)?;
-    let input = request
-        .input
-        .ok_or_else(|| TerminalError::new("missing_input", "Missing terminal input."))?;
+    let input_bytes = match (request.input, request.input_bytes) {
+        (Some(input), None) => input.into_bytes(),
+        (None, Some(bytes)) => bytes,
+        (Some(_), Some(_)) => {
+            return Err(TerminalError::new(
+                "invalid_request",
+                "Provide either data or data_b64, not both.",
+            ))
+        }
+        (None, None) => {
+            return Err(TerminalError::new(
+                "missing_input",
+                "Missing terminal input.",
+            ))
+        }
+    };
     let manager = terminal_manager();
     let session = {
         let manager = manager
@@ -1077,7 +1095,7 @@
     }
     session
         .writer
-        .write_all(input.as_bytes())
+        .write_all(&input_bytes)
         .map_err(|error| TerminalError::new("write_failed", error.to_string()))?;
     session
         .writer


thinking
**Committing updated run log**

The run log file changed after the previous commit, so I'll stage and create a new commit to include this update per the requirement that all changes during the run must be committed.
exec
/bin/zsh -lc 'git add /Users/mac/codes/vibe-inspect/.ralph/runs/run-20260201-231810-31541-iter-6.log' in /Users/mac/codes/vibe-inspect